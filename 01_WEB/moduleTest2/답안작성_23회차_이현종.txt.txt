문제 1
1-1) 라우터 테이블을 완성하세요.
    { path: '/', name: 'home', component: HomePage },
    { path: '/travel/list', component: TravelListPage },
    {
      path: '/travel/view/:id',
      name: 'travel/view',
      component: TravelDetailPage,
    },
    { path: '/travel/write', component: TravelWritePage },


1-2) RouterLink를 사용하여 관광지 목록 페이지로 이동할 수 있는 링크를 생성하세요.
    <RouterLink to="/travel/list"> 관광지 목록으로 이동</RouterLink>


문제 2. src/pages/TravelListPage.vue에서는 관광지 목록을 서버로부터 받아, TravelList 컴포넌트로 전달한다. 스크립트 파트를 완성하세요.
2-1) load() 함수를 완성하세요.
    const response = await axios.get(BASE);
    state.travels = response.data;


2-2) TravelList 컴포넌트를 배치, travles 프로퍼티 설정
    <TravelList :travels="state.travels"></TravelList>


문제 3. src/components/TravelList.vue, TravelItem.vue 컴포넌트를 완성하세요.
3-1). travels 프로퍼티를 정의하세요.
    const props = defineProps(['travels']);



3-2) travels 프로퍼티로 TravelItem 컴포넌트를 관광지수 만큼 렌더링 하세요.
    <TravelItem v-for="travel in travels" :key="travel.id" :travel="travel" />


3-3) 관광지의 이미지를 출력하세요. 이미지의 폭과 높이는 20px로 합니다. 이미지가 없다면 출력하지 않습니다.
    <img
      v-if="travel.images[0]"
      :src="travel.images[0]"
      style="width: 20px; height: 20px"
    />


문제 4. 관광지 상세 보기 페이지의 스크립트 코드를 완성하세요.
4-1) router를 설정하세요.
    const router = useRouter();

4-2) currentRoute를  설정하세요.
    const currentRoute = useRoute();

4-3) 라우트 파라미터에서 id 추출하여 id를 설정하세요.
    const id = parseInt(currentRoute.params.id);

4-4) 반응형 데이터 travel 정의하세요.
    const travel = ref([]);

4-5) 데이터 로드 함수를 완성하세요.
    const response = await axios.get(`${BASE}/${id}`);
    travel.value = response.data;


4-6) 돌아가기 버튼 이벤트 핸들러 함수를 정의하세요.
    const back = () => router.push({ path: '/travel/list' });


4-7) 관광지의 이미지를 출력하세요. 관광지 이미지의 폭 크기를 150px로 하세요. 
    <img v-for="image in travel.images" :src="image" style="width: 150px" />



문제 5. src/pages/TravelWritePage.vue 관광지 등록 코드를 완성하시오.
5-1) router를 설정하세요.
    const router = useRouter();

5-2) reactive로 반응형 데이터 travel를 정의하세요.
    const travel = reactive({ images: [] });

5-3) submit 이벤트 핸들러에서 "등록하시겠습니까?" 물어보고 예를 누른 경우에 등록하도록 하세요.
    if (confirm('등록하시겠습니까?')) {...}


5-4) submit 이벤트 핸들러 함수를 완성하세요.
    const payload = travel;
    axios.post(BASE, payload);
    back();


5-5) 돌아가기 버튼 이벤트 핸들러 함수를 정의하세요.
    const back = () => router.push({ path: '/travel/list' });

5-6) 템플릿에서 submit 이벤트 핸들러를 등록하세요.
    <input type="submit" value="확인" @click="submit" />